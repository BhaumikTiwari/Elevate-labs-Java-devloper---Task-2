create 2 classes: Student and StudentManager

Student → holds id, name, marks (data only)

StudentManager → main logic, menu, ArrayList

file name should be StudentManager.java

only StudentManager class is public
